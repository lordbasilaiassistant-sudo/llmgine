## What changed

## What runs differently (paste test/demo output)

## Checklist
- [ ] `npm run typecheck && npm test` pass
- [ ] New logic has colocated tests
- [ ] `core` stays LLM-free; LLM features have a deterministic fallback
- [ ] Conventional commit messages
